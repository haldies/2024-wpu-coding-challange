const segita = (ada) => {
    for(let i = ada; i >= 1; i--){
        let str = ' '.repeat(ada-i);
        let str2 = '*'.repeat(i*2 -1)
        console.log(str + str2 + str);
    } 
    for(let i = 1; i <= ada; i++){
        let str = ' '.repeat(ada-i);
        let str2 = '*'.repeat(i*2 -1)
        console.log(str + str2 + str);
    } 
    for(let i = ada; i >= 1; i--){
        let str = ' '.repeat(ada-i);
        let str2 = '*'.repeat(i*2 -1)
        console.log(str + str2 + str);
    } 
    for(let i = 1; i <= ada; i++){
        let str = ' '.repeat(ada-i);
        let str2 = '*'.repeat(i*2 -1)
        console.log(str + str2 + str);
    } 
}




